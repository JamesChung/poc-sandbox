#!/usr/bin/env python
# from checkov.main import Checkov
# TODO: Override with CheckovWrapper during invocation
from checkov.main_wrapper import CheckovWrapper
import warnings
import sys

if __name__ == '__main__':
    with warnings.catch_warnings():
        warnings.simplefilter("ignore", category=SyntaxWarning)
        # print(sys.path)
        sys.exit(CheckovWrapper().run())
        # sys.exit(Checkov().run())
